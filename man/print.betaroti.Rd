% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betaroticore.R
\name{print.betaroti}
\alias{print.betaroti}
\title{Print}
\usage{
\method{print}{betaroti}(x, digits = max(3, getOption("digits") - 2), ...)
}
\arguments{
\item{x}{Um objeto das classes 'betaroti' ou 'betarotidv'.}

\item{digits}{Número de dígitos significativos para a impressão dos valores (padrão é max(3, getOption("digits") - 2)).}

\item{...}{outros parametros}
}
\description{
Esta função imprime um resumo do objeto das classes 'betaroti' e 'betarotidv' fornecido, incluindo estimativas, erros padrão, valores t e valores-p.
}
\examples{
\dontrun{
# Exemplo de uso da função hessian
# Primeiro, gere um objeto de classe 'betaroti' ou 'betarotidv'
set.seed(42)
n <- 100
dados <- data.frame(
  x1 = rnorm(n, mean = 1, sd = 0.5),
  x2 = rbinom(n, size = 1, prob = 0.5),
  x3 = rnorm(n, mean = 2, sd = 1))
betas <- c(0.2, 0.3, -0.4, 0.1)
formula <- ~x1 + x2 + x3
phi <- 50
dados_simulados <- beta_ordinal_simula_dados(
  formula = formula,
  dados = dados,
  betas = betas,
  phi = phi,
  link = "logit",
  ncuts = 100)
fit <- beta_ordinal_fit(
 formula = formula,
 dados = dados_simulados,
 link = "probit",
 num_hessiana = TRUE)
print(fit)
}
}
