% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loglik.R
\name{betaregscale_loglik_z}
\alias{betaregscale_loglik_z}
\title{Log-likelihood for variable-dispersion beta interval regression}
\usage{
betaregscale_loglik_z(
  param,
  formula = y ~ x1 + x2 | z1,
  data,
  link = "logit",
  link_phi = "logit",
  ncuts = 100L,
  type = "m",
  lim = 0.5,
  repar = 2L
)
}
\arguments{
\item{param}{Numeric vector of length \eqn{p + 1}: the first
\eqn{p} elements are the regression coefficients \eqn{\beta},
and the last element is the (link-scale) dispersion parameter.}

\item{formula}{One-sided or two-sided formula for the mean model.}

\item{data}{Data frame containing the response and predictors.}

\item{link}{Character: link function for the mean (default
\code{"logit"}).}

\item{link_phi}{Character: link function for the dispersion
(default \code{"logit"}).}

\item{ncuts}{Integer: number of scale categories (default 100).}

\item{type}{Character: interval type (\code{"m"}, \code{"l"},
or \code{"r"}).}

\item{lim}{Numeric: half-width of uncertainty region (default
0.5).}

\item{repar}{Integer: reparameterization scheme (0, 1, or 2;
default 2).}
}
\value{
Scalar: total log-likelihood.
}
\description{
Computes the total log-likelihood for a beta regression model with
mixed-censored responses and observation-specific dispersion
governed by a second linear predictor.  Uses the compiled C++
backend.
}
\details{
The formula should use the \code{\link[Formula]{Formula}} pipe
notation: \code{y ~ x1 + x2 | z1 + z2}, where the left-hand side
of \code{|} defines the mean model and the right-hand side defines
the dispersion model.
}
\examples{
set.seed(42)
n <- 100
dat <- data.frame(
  x1 = rnorm(n), x2 = rnorm(n),
  z1 = runif(n)
)
sim <- betaregscale_simulate_z(
  formula_x = ~ x1 + x2, formula_z = ~z1,
  data = dat,
  beta = c(0.2, -0.5, 0.3), zeta = c(0.5, -0.5)
)
betaregscale_loglik_z(
  param = c(0.2, -0.5, 0.3, 0.5, -0.5),
  formula = y ~ x1 + x2 | z1, data = sim
)

}
