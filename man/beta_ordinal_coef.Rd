% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betaroticore.R
\name{beta_ordinal_coef}
\alias{beta_ordinal_coef}
\title{Coleta estatística do ajuste}
\usage{
beta_ordinal_coef(fit, alpha = 0.05)
}
\arguments{
\item{fit}{Objeto do sjuste retornado das funções \link{beta_ordinal_fit} e
\link{beta_ordinal_fit_z}}

\item{alpha}{Nível de significância do alpha para os intervalos de confiança.
Padrão 0.05 bilateral.}
}
\value{
Lista contendo as estimativas coeficientes, suas estatísticas e a bondade do ajuste.
}
\description{
Coleta as estimativas e suas estatísticas para um objeto de ajuste
do modelo beta ordinal com censura intervalar. Coleta também as estatísticas
de bondade do ajuste como a log-verossimilhança, o AIC e o BIC do modelo
}
\examples{
# Simulando dados
n <- 50
fx <- ~ x1 + x2
fz <- ~ z1
dados <- data.frame(
  x1 = rnorm(n),
  x2 = rnorm(n),
  z1 = runif(n),
  z2 = runif(n)
 )
 dados_simulados <- beta_ordinal_simula_dados_z(
   formula_x = fx,
   formula_z = fz,
   dados = dados,
   betas = c(0.2,-0.5, 0.3),
   zetas = c(1, 1.2),
   link_x = "logit",
   link_z = "log",
   ncuts = 100
   )

 fit_z <- beta_ordinal_fit_z(
  formula_x = fx,
  formula_z = fz,
  dados = dados_simulados,
  link_x = "logit",
  link_z = "log",
  num_hessiana = TRUE
  )

coe <- beta_ordinal_coef(fit_z)
coe$est
coe$gof
}
