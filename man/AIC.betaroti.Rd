% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betaroticore.R
\name{AIC.betaroti}
\alias{AIC.betaroti}
\title{Critério de Informação de Akaike (AIC)}
\usage{
\method{AIC}{betaroti}(object, ..., k = 2)
}
\arguments{
\item{object}{Um objeto das classes 'betaroti' ou 'betarotidv'.}

\item{...}{Argumentos extras}

\item{k}{Penalidade por parâmetro a ser utilizado; o padrão k = 2 é o AIC clássico.}
}
\description{
Esta função calcula o Critério de Informação de Akaike (AIC) para objetos das classes 'betaroti' e 'betarotidv'.
}
\examples{
\dontrun{
# Exemplo de uso da função AIC
# Primeiro, gere um objeto de classe 'betaroti' ou 'betarotidv'
set.seed(42)
n <- 100
dados <- data.frame(
  x1 = rnorm(n, mean = 1, sd = 0.5),
  x2 = rbinom(n, size = 1, prob = 0.5),
  x3 = rnorm(n, mean = 2, sd = 1))
betas <- c(0.2, 0.3, -0.4, 0.1)
formula <- ~x1 + x2 + x3
phi <- 50
dados_simulados <- beta_ordinal_simula_dados(
  formula = formula,
  dados = dados,
  betas = betas,
  phi = phi,
  link = "logit",
  ncuts = 100)
fit <- beta_ordinal_fit(
 formula = formula,
 dados = dados_simulados,
 link = "probit",
 num_hessiana = TRUE)
# Em seguida, use a função AIC
aic_result <- AIC(fit)
}
}
